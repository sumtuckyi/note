브라우저는 HTML 문서를 해석하여 DOM tree라는 객체 트리로 구조화
= 웹 페이지의 문서 구조를 표현하는 객체 모델 
-> 웹 페이지의 요소와 그 속성 또는 스타일에 접근하여 조작할 수 있도록 트리 형태의 객체 모델을 제공하는 것
자바스크립트는 스크립트 언어로, 인터프리터가 코드를 한 줄씩 읽고 해석, 실행하므로 일종의 인터프리터 언어라 할 수 있다.
브라우저와 웹 서버 모두에서 실행된다. 
자바스크립트 코드를 해석하는 것은 자바스크립트 엔진인데, 브라우저에는 이 엔진이 내장되어 있다. 
브라우저는 자바스크립트의 클라이언트 실행 환경이고, 
서버 실행환경으로는 대표적으로 node가 있다.  


※스크립트 언어
: 기존에 존재하는 소프트웨어를 제어하기 위한 용도로 쓰이는 언어
※컴파일 언어
: 코드가 컴파일러를 거쳐 기계어로 번역된 후(실행파일)에 실행되는 언어
인터프리터 언어(cpu에서 실행될 때 코드가 한 줄 씩 기계어로 해석됨)와 달리 인터프리터를 거치지 않아 속도가 상대적으로 빠르다. 

※비동기 프로그래밍 
어떤 자원에 대한 요청을 하고 응답을 기다리는 동안 다른 코드를 작업을 실행할 수 있다.
※ single-threaded language
it executes the current line of code before moving to the next.
JavaScript being non-blocking
asynchronous functions 

prototype
class(객체 지향 관련)
자바스크립트에는 static types가 없다. 

상속과 관련하여, 자바스크립트에는 객체(object)라는 하나의 구조만 존재한다.
각 객체는 다른 객체와 연결되는 private한 속성을 가지는데 이를 prototype이라 한다. 
prototype object는 자신만의 prototype을 가진다. 
객체는 'null'을 프로토타입으로 가지게 될 때까지 프로토타입 체인을 타고 연결된다. 
object의 property에 접근할 때, 해당 property는 해당 object에서만 찾는 것이 아니라 
해당 오브젝트의 프로토타입, 해당 프로토타입의 프로토타입 이런식으로 연쇄적으로 
찾아나간다. 언제까지? 해당 프로토타입 체인의 끝 또는 일치하는 이름의 속성을 찾을 때까지

class-based languages와 달리 자바스크립트는 메서드를 다른 방식으로 정의한다.




프로토타입 상속 모델
JavaScript objects have a link to a prototype object.