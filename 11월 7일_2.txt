{ 
// 프로젝트 이름
  "name": "vue-project",
// 프로젝트의 버전 [major].[minor].[patch] 
  "version": "0.0.0",
// npm storage에 해당 프로젝트를 배포할 수 있는지 여부
  "private": true,
// 실행 스크립트를 정의
  "scripts": {
    "dev": "vite", // 개발 서버 실행
    "build": "vite build", // 프로젝트를 배포할 수 있는 형태로 만들어줌
    "preview": "vite preview" // 배포한 프로젝트 미리보기
  },
// 배포 환경에서 필요한 패키지를 정의
// 앞에 '~'가 붙은 경우, 작성된 버전보다 높거나 같고, 다음 마이너 버전보다 낮은 버전 내에서 자동으로 업데이트
// 앞에 '^'가 붙은 경우, 작성된 버전보다 높거나 같고, 다음 메이저 버전보다 낮은 버전 내에서 자동으로 업데이트
  "dependencies": {
    "three": "^0.158.0",
    "vue": "^3.3.4"
  },
// 개발 환경에서 필요한 패키지를 정의
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "vite": "^4.4.11"
  }
}