pjt05부터는 어떤 주제로 관통프로젝트를 진행할 지 결정해야함...

django template 그래프 출력
-> matplotlib 패키지를 설치하여 구현 

app > templates > appname 의 계층 구조로 폴더를 생성하는 것은 앱이 다수인 경우 template 이름이 같을 때,
어떤 앱의 템플릿 파일인지 구분하기 위함이다.

view에서 template으로 이미지 파일 전달하기 : 이미지 형식의 데이터를 직접 전달하는 것은 안 되고, 저장된 이미지의 경로를 전달해
출력해왔음
♥1.static
♥2.media
-> 파일 시스템을 통하지 않는 다른 방법은? ♥3. '임시 저장' 즉, 버퍼를 활용 -> 내장 모듈과 클래스 활용

※buffer : 파일시스템과 유사하지만 실제로 파일로 만들지 않고 메모리 단에서 작업할 수 있다. 
-plot -> 이진 데이터 -> 버퍼에 저장 -> 저장된 주소를 템플릿에 전달


참고) 터미널 에러 plt를 만드는 곳(main thread)과 그래프를 그리는 곳이 달라서 에러가 발생할 수 있음을 경고 


grey : #F6F8FA
blue : #0550BE
orange : #FD8C73
white : #FFFFFF
violet : #CA94DF

ver1 - pandas, matplotlib활용
: 데이터 파일의 데이터를 전처리하여 출력하기

ver2 - 인증페이지 구현 
: 영화 데이터의 생성, 조회, 수정, 삭제
로그인, 로그아웃, 회원가입 등의 기능 구현 
★★내장된 User model을 그대로 사용하지 않고 커스텀하여 덮어쓰는 이유?★★
=> 장고 사용 시 권장됨 + 원하는 추가적인 필드를 생성하여 사용할 수 있기 때문 
-> models.py에서 class User정의 시, 원하는 추가적인 필드를 정의 + forms.py에서 User model을 기반으로 만들어진 
Form에서도 새로운 필드를 정의해주어야 정상적으로 작동 